<div class="row">
  <div class="twelve columns">
    <nav>
      <a href="/"><img class="nav-bar" src="/rsc/pokedexLogo.png" alt="" /></a>

      <div class="nav-bar" id="nav-btns">
        <% if(locals.index && index){ %>
        <button
          class="nav-btn"
          type="button"
          onClick="listPokemons('<%= userId %>')"
        >
          Listar Pokemones
        </button>
        <% if(userName){ %>
        <button
          type="button"
          onClick="listFavourites('<%= userId %>')"
          class="nav-btn"
          style="margin-right: 30em;"
        >
          Listar Favoritos
        </button>
        <!-- <button
          type="button"
          onClick="listTeam('<%= userId %>')"
          class="nav-btn"
          style="margin-right: 30em;"
        >
          Listar Equipo
        </button> -->
        <% } %> <% } %> <% if(!userName){ %>
        <a href="/auth/register">Registrarse</a>
        <a href="/auth/login">Iniciar Sesión</a>
        <% } %> <% if(userName){ %>
        <p id="userdata" data-userid="<%= userId %>"><strong><%= userName %></strong></p>
        <a href="/auth/logout">Cerrar Sesión</a>
        <% } %>
      </div>
    </nav>
  </div>
</div>
